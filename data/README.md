# 固定与脱敏数据集

开发、测试和评测数据必须可追溯、脱敏、固定版本。推荐按 `market/`、`products/`、`reviews/`、`sales/`、`inventory/`、`knowledge/` 与 `evaluation/` 分目录，并为每个快照记录 `source_timestamp`、`ingest_timestamp`、口径版本和授权说明。

禁止提交真实客户密钥、支付信息、无必要的个人信息或未经授权的平台采集数据。
